import React from "react";
import Image from "next/image";

export function PostHeader({ user }) {
  return (
    <div>postheader</div>
    // <div className="content--inner header__user">
    //   <Image className="profile__profile-picture" src={user.profilePicture} alt="" width={24} height={24} />
    //   <span className="profile__name">{user.name}</span>
    // </div>
  );
}
